---
description: Run all local Terraform quality gates (fmt, validate, tflint, gitleaks)
---

Use the tf-check skill to run this repo's Terraform quality gates on both the
`infra/` and `bootstrap/` stacks. Report the result of each gate explicitly.

$ARGUMENTS
